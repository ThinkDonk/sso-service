services:
  sso:
    build: .
    env_file: .env
    environment:
      SSO_HOST: "0.0.0.0"
      SSO_DB_PATH: "/app/data/sso.db"
    ports:
      - "${SSO_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
